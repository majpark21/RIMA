% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_matches_embed.R
\name{plot_matches_embed}
\alias{plot_matches_embed}
\title{Plot Neighbourhood Matches in Side-by-Side Embeddings}
\usage{
plot_matches_embed(
  milos,
  dt_match,
  cols_color = c("celltype", "celltype"),
  dt_palette = NULL,
  dimred = "UMAP",
  args_process_coordinates = list(list(angle = 0, shift = c(0, 0)), list(angle = 0, shift
    = c(10, 0))),
  add_non_matched_category = c(),
  sim_limits = NULL,
  title = "",
  linewd = 0.35
)
}
\arguments{
\item{milos}{List of 2 Milo objects.}

\item{dt_match}{A data.table with 3 columns containing the nhood-nhood matching and similarities.
Typically generated by \code{match_nhoods()}.}

\item{cols_color}{A character vector of length 2 specifying column names from \code{colData}
used to color the neighbourhoods. If \code{NULL}, neighbourhoods are displayed in black.}

\item{dt_palette}{A data.table with 2 columns (one named after \code{cols_color} containing
neighbourhood labels, one named \code{"color"} containing HEX color codes),
or \code{NULL} to use default colours.}

\item{dimred}{Character string specifying which reduced dimension to visualize.
Default is \code{"UMAP"}.}

\item{args_process_coordinates}{A list of 2 lists specifying coordinate preprocessing.
Each nested list contains \code{angle} (rotation angle in degrees) and
\code{shift} (numeric vector of length 2 for horizontal and vertical offset).}

\item{add_non_matched_category}{Ignored, not implemented yet.}

\item{sim_limits}{Numeric vector of length 2 specifying colour scale limits for similarities.
If \code{NULL}, limits are determined from the data.}

\item{title}{Character string for the plot subtitle.}

\item{linewd}{Numeric. Width of the matching connecting lines. Default is \code{0.35}.}
}
\value{
A \code{ggplot} object displaying the matched neighbourhoods.
}
\description{
Visualizes one-to-one neighbourhood matches between two datasets by plotting their
reduced dimension embeddings side-by-side with connecting lines showing the matches.
}
\examples{
# Not run: plot_matches_embed(milos, dt_match, cols_color = c("celltype", "celltype"))
}
